<?php
// you dont use the same names given in the exapmle!!!!!
// $item = (EMPTY);
$item2 = 'words';
// $array_item = array(1,2,3);



// Create a function that checks if a variable is set or empty, and display "$variable_name is SET|EMPTY"
var_dump( unserialize($item2)); 

// TEST: If var $item is set, display '$item is SET'
// //bol(true)

// TEST: If var $item is empty, display '$item is EMPTY'
// //bol(true)

// TEST: If var $item2 is set, display '$item2 is SET'
// //bol(true)

// Serialize the array $array_item, and output the results
// //bol(true)

// Unserialize the array $arra_item, and output the results
